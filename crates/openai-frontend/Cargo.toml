[package]
name = "openai-frontend"
edition.workspace = true
license.workspace = true
version.workspace = true
description = "OpenAI-compatible HTTP frontend shared by mesh-llm runtimes"
repository = "https://github.com/Mesh-LLM/mesh-llm"
homepage = "https://github.com/Mesh-LLM/mesh-llm"

[dependencies]
async-trait = "0.1"
axum = "0.8"
futures-core = "0.3"
futures-util = "0.3"
mesh-llm-guardrails = { path = "../mesh-llm-guardrails", version = "0.72.1"  }
mesh-llm-events = { path = "../mesh-llm-events", version = "0.72.1" }
serde.workspace = true
serde_json.workspace = true
tokio = { version = "1", features = ["time"] }
tokio-stream = "0.1"
tracing = "0.1"
uuid = "1"

[dev-dependencies]
http-body-util = "0.1"
tokio = { version = "1", features = ["macros", "rt", "time"] }
tower = { version = "0.5", features = ["util"] }
